# 爆笑スタジアム｜仕様書 v2（実装ベース、2026-09-02）

> **この文書について**：旧 `仕様書.md`（2026-07-12作成、企画初期の構想ベース）は、その後の実装でほぼ全面的に上書きされており、現在のアプリとは一致しない箇所が非常に多くなっています。本書はコード（`src/`・`supabase/migrations/`）を実際に読んで、**今動いているものだけ**を基準に書き直したものです。旧仕様書は削除せず残しますが、内容が古いため参照しないでください。
>
> 本書に書いていないことは「未実装」または「確認できなかった」ものです。企画上の願望・将来案は §12（今後の展開）にまとめ、本文（§1〜§11）とは明確に分けています。

---

## 0. アプリ名と世界観

- **正式名称：「爆笑スタジアム」**（2026年8月末に「大喜利道場」から改名済み。ソースコード上「大喜利道場」の文字列は残っていない）。
- コンセプト：1日1〜2回の定時開催、5人前後の組でお題に回答し合い、他の演者が採点する「オンライン大喜利ライブ大会」。
- デザインは現在**3系統が併存**しています（§10で詳述）。統一の是非は今後の検討事項です。

---

## 1. ライブの進行フロー

### 1.1 フェーズ一覧と実際のタイミング

`lives.current_phase` が進行状態を表します。カッコ内は`src/data/liveRoomTiming.ts`の本番用実測値です。

| フェーズ | 内容 | 長さ |
|---|---|---|
| `scheduled` | 予定登録済み、まだ開始していない | — |
| `interlude` | 幕間演出（暗転→タイトル、`CurtainOverlay`） | 15秒 |
| `opening` | 参加登録受付中（プレイヤー/観客を選ぶ、§2） | 45秒（運営が任意のタイミングで次へ進められる） |
| `topic_reveal` | その組・その周のお題発表 | 13秒 |
| `answering` | 回答受付（後述の審査サイクルを内包） | 60秒（送信ごとに一時停止） |
| `group_result` | その組のこの周の結果発表 | 30秒 |
| `final_result` | 全周終了後の個人総合結果発表 | 運営が終了操作をするまで |
| `closed` | 終了。段位・ポイント加算、寄合帳への結果掲載が可能になる | — |

- 上記の秒数は**タイマー表示の長さ**であり、実際の遷移はホスト（司会役）のブラウザが`phase_deadline`（サーバー時刻ベース）を見て自動で進めます。**ボタンを押さなくても時間が来れば自動で次のフェーズに進む**仕組みですが、これは「司会コンソールを開いているブラウザが裏で監視している」からで、サーバー側で完全自動化されているわけではありません（司会のブラウザが閉じている間は進行が止まります）。
- 節目の操作（受付開始、組分け確定、ゲーム開始、ライブ終了）は司会が手動でボタンを押します。

### 1.2 回答の審査サイクル（`answering`フェーズの中身）

1. 舞台の組のプレイヤーが回答を送信すると、持ち時間（`phase_deadline`）が**一時停止**する（`lives.answering_paused=true`）。
2. 少し間を置いてから（`revealDelayMs` 2.2秒）、その回答が画面中央に表示される（`answers.revealed_at`をセット）。
3. **他の組のプレイヤー**（自分の組が舞台にいない人）が採点する。持ち時間は`judging_ends_at`から**10秒**（`judgeMs`）。全員が投票し終えたら10秒を待たず早期確定する。
4. 採点が確定すると（`answers.resolved=true`）、得点演出（玉が弾ける→得点表示、合計 約3.7秒の`REVEAL_SEQUENCE_MS`）が入り、この間は`lives.reveal_sequence_until`によって新しい回答の送信がロックされる。
5. 演出が終わると持ち時間が再開する。

- **1人最大5回答**（`MAX_ANSWERS_PER_PLAYER`）まで送信可能。1ターンにつき「まだ確定していない回答」は常に1件まで（DBの一意制約で保証）。
- 回答の文字数上限は**50文字**（`MAX_ANSWER_BODY_LENGTH`）。
- 組の全員が5回答を使い切ったら、持ち時間を待たず次のフェーズへ進む。

### 1.3 周回（ラウンド）

- 全組が1回ずつ舞台に立つのを1周とする。**デフォルトの周回数は1周**（`ROUNDS_PER_LIVE_DEFAULT`、2026-08-27に2周から変更）。この値は定数であり、ライブごとに運営がUIから変更する機能はありません。
- 組ごと・周ごとに別のお題が必要（`groupCount × ROUNDS_PER_LIVE_DEFAULT`件、司会がお題バンクからランダムまたは手動選択で用意）。

---

## 2. 参加登録・組分け

### 2.1 参加登録

- ログイン（X OAuth）していれば誰でも`join_live`という関数を通じて参加登録できる。「プレイヤーとして参加する」「観客として参加する」のどちらかを選ぶ（`preferred_role`）。
- **プレイヤー枠には上限がある**（`lives.max_players`、司会がライブ準備時に設定。UIの初期値は15人・3組=5人/組だが、これは単なる初期値でありライブごとに自由に変更できる）。上限に達すると、それ以降にプレイヤー参加を希望した人は拒否され、観客としてなら参加できる。
- **観客には人数上限がない**。
- 一度「見学希望」で登録した人が後から「プレイヤー希望」に変更することも、上限内であれば可能。
- 利用停止中（後述§9.4）のアカウントは新規のライブ参加登録自体ができない。ただし進行中のライブから追い出されることはない（既に参加登録済みの人への遡及処置はしない）。

### 2.2 組分け

- 「プレイヤー希望」で登録した全員を対象に、司会が「ランダムに振り分ける」を押すと、指定した組数（`groups`テーブル、1〜8組を想定したUI）へランダムに均等分配される（余りは先頭の組から1人ずつ）。
- 組分けは開幕後に何度でもやり直せる（やり直すと既存の割当は一旦リセットされる）。個別の参加者だけ手動で組を変更することも可能。
- 見学希望のまま登録した人、および上限超過でプレイヤーになれなかった人は`role='audience'`のまま。**「N人以上なら観客専任枠」のような人数連動の自動編成ロジックは存在しない**（旧仕様書にあった記述は撤回）。
- 最低開催人数のシステム的な下限チェックは存在しない。何人集まったら開催するかは運営の裁量。

---

## 3. 採点方式

- 採点は**0・1・2・3点の4段階**（**これが正式仕様**。3段階に戻すことは想定していません）。
- 採点できるのは「別の組に所属するプレイヤー」のみ。**観客（audience）役は一切採点できない**。自分の回答への自己採点も不可。
- 1回答につき1採点者1回のみ（一意制約）。一度投票すると変更不可（一発勝負）。
- 「大ウケ」演出：ある回答について、**採点者の過半数が最高点(3点)をつけた**場合に発生（`top_score_votes > floor(採点者数 / 2)`）。
- ライブ内スコア＝自分の全回答の得点合計。

### 3.1 サーバー側の不正防止（2026-09-02強化）

画面のボタン操作を経由せず、Supabaseへ直接リクエストを送っても以下は全て拒否されます。
- 表示前（`revealed_at`が空）の回答への採点
- 確定済み（`resolved=true`）の回答への採点
- 採点期限（`judging_ends_at`、若干の猶予つき）を過ぎた採点
- 現在進行中のターン・フェーズと一致しない回答/採点対象
- 回答受付の一時停止中（`answering_paused`）や、得点演出中（`reveal_sequence_until`）の回答送信

---

## 4. 経済システム：段位・ポイント・寄合券

旧仕様書にあった「消費用ポイント／熟練度メーターの2軸」という設計は、**2026年9月に1本の式へ統一**されました。

### 4.1 統一された加算式（ライブ終了時、`apply_live_rank_rewards`が自動実行）

**プレイヤーのみ**が対象（観客はポイント・段位ともに加算なし）。

```
獲得量 = 参加ボーナス10pt + そのライブでの獲得スコア + 順位ボーナス
順位ボーナス：1位+100 / 2位+60 / 3位+30 / 4位以下0
```

この「獲得量」が、以下**3つの値に同時に**加算されます（同じ数字が3箇所に入る）。

| 値 | 意味 | 増減 |
|---|---|---|
| `profiles.mastery_meter`（熟練度メーター） | 段位判定に使う経験値 | 増える一方、減らない |
| `profiles.total_points`（累計ポイント） | 通算の実績表示用 | 増える一方、減らない |
| `profiles.points_balance`（ポイント残高） | 将来の消費用途を見込んだ残高 | 現状は消費先が無いため実質増える一方 |

- 旧仕様書にあった「1位300pt/2位200pt/3位100pt」というポイント専用の式、および「ベストアンサー+100pt」の自動ボーナスは**廃止**されています。

### 4.2 運営ベスト（自動のベストアンサーボーナスの代替）

- 自動で最高得点者に+50を付与する仕組みは廃止しました。代わりに、**運営が寄合帳のライブ結果ページで「運営ベスト」を選ぶと、選ばれた本人に+50**（mastery_meter・total_points・points_balance全てに）が付与されます。
- 付与は選ばれた回答ごとに**一度きり・永続**：運営が後で選び直しても、既に付与した+50は取り消されません（同じ回答が再度選ばれた場合の二重付与だけを防止）。
- 選ばれた人にはベル通知（`type='manager_best'`）が届きます。

### 4.3 段位（8段階）

`profiles.mastery_meter`の累積値で自動判定（`getRankByMeter`）。

| 段 | 呼称 | 閾値 |
|---|---|---|
| 1 | 見習い | 0 |
| 2 | 前座 | 150 |
| 3 | 二ツ目 | 400 |
| 4 | 真打 | 900 |
| 5 | 花形真打 | 1,800 |
| 6 | 大看板 | 3,200 |
| 7 | 名人 | 5,500 |
| 8 | 達人 | 9,000 |

### 4.4 寄合券（`profiles.tickets_count` / `tickets_next_recovery_at`）

- 寄合帳への**お題投稿・回答投稿**に1枚消費するスタミナ制リソース。**コメント（ツッコミ）は消費しない**。
- 最大5枚、1枚消費すると1時間後に1枚回復。
- **サーバー側で完全管理**（2026-09-02にローカル管理から移行）。投稿の保存と券の消費は1つのデータベース関数の中で原子的に行われ、投稿が失敗すれば券は減らない。0枚では投稿自体がサーバー側で拒否される。

### 4.5 現状「近日公開」で非表示の経済機能

以下は**実装自体は残っているが、画面上は「近日公開」表示に差し替えて非表示にしている**（2026-09-01）。理由は、これらが上記の実データ経済（§4.1〜4.4）とは無関係の**ローカルのみのダミー通貨**（`useUserStore`、初期値5000pt）で動いており、実際に貯まるポイントと無関係だったため。

- ガチャ（`/gacha`）
- 衣装蔵・ショップ（同ページ内タブ）
- 楽屋・着せ替え（`/backstage-room`）
- 番付表・過去のライブ（`/ranking`）

再解禁するには、まずこれらをローカル経済ではなく`profiles.points_balance`ベースに作り直す必要があります（§12参照）。

---

## 5. アカウント・認証

- ログインはX（旧Twitter）OAuth 2.0のみ。ゲスト参加・メールログインは無い。
- 初回ログイン時、`profiles`行がトリガーで自動生成され、演者名（初期値はXの表示名）を決めるモーダルが表示される。
- 運営権限は`profiles.role`（`'user' | 'admin'`）で判定。DBから手動で立てる必要があり、アプリ内に自己昇格の手段はない。
- 利用停止（`profiles.suspended_until` / `is_permanently_suspended`）は運営が`/admin/users`から操作する。停止中は寄合帳への投稿・新規ライブ参加登録ができない（進行中のライブへの遡及処置はしない、§2.1参照）。

---

## 6. 寄合帳（SNS機能）

旧仕様書には一切記載がありませんが、実際にはかなり大きな機能です。`/sns`配下、マイページからも一部埋め込み表示。

- **お題投稿**（`sns_topics`、寄合券1枚消費、60文字まで）
- **回答投稿**（`sns_answers`、寄合券1枚消費、80文字まで）
- **ツッコミ＝コメント**（`sns_comments`、寄合券は消費しない、60文字まで）
- **いいね**（`sns_answer_likes`）
- **フォロー/フォロワー**（`sns_follows`）
- **プロフィール**：自分・他人の演者名/段位/一言コメント（`bio`）を表示。他ユーザーの`profiles`は直接見えない設計のため、`sns_author_names`という専用の関数経由で必要な項目だけ取得する。
- **報告（通報）機能**：お題・回答・コメントに対して通報でき、運営が`/admin/reports`・`/admin/posts`でレビューする。

### 6.1 ライブ結果の掲載（寄合帳内「ライブ結果」タブ）

- 終了済み（`current_phase='closed'`）かつ運営が公開設定（`lives.results_published`）したライブについて、以下を寄合帳に掲載：
  - 1〜3位の最高得点回答（同点は全員分）
  - 満点回答（審査員全員が最高点をつけた回答）
  - 運営が選ぶ「運営ベスト」（§4.2）
- いいね・コメントも可能。運営専用の管理画面（`/admin/live-results/[liveId]`）で掲載内容の調整・除外・運営コメント追加ができる。

投稿内容（お題・回答・コメント本文、表示名等）には長さ制限をRLS/DB制約でも設けている（想定より極端に長い文字列を直接APIで送っても拒否される）。

---

## 7. 通知（ベル通知）

`notifications`テーブル、画面右上のベルアイコンから確認。現在使われている種類：

| type | 内容 | 誰が受け取るか |
|---|---|---|
| `warning` | 運営からの個別警告 | 対象ユーザー |
| `announcement` | 運営からの一斉お知らせ配信 | 配信対象の全ユーザー |
| `manager_best` | 運営ベストに選ばれたお知らせ（+50pt） | 選ばれた本人 |

運営は`/admin/notifications`から、警告・お知らせ配信・運営ベスト通知のそれぞれを個別に非表示にできる（本人には見えなくなるが記録は残る）。**プッシュ通知（ブラウザを閉じていても届く仕組み）は無い**。開いた時にだけ確認できる。

---

## 8. Xシェア機能

3箇所にXへの投稿ボタンがある（2026年9月追加、いずれもXの投稿画面を開くだけでAPI連携は無い）。

1. ライブ予定ページ（`/live-schedule`）の「今回のライブ」カード：開催の事前告知用
2. 寄合帳のライブ結果ページ：結果発表の事後シェア用
3. ライブ終了直後の最終結果画面：個人の結果シェア用（自分の順位のみ、他人の順位・得点は含めない）

クリック回数は`share_click_events`に記録され、運営のみ閲覧可能（一般ユーザーは見れない）。未ログインでの記録・連打はサーバー側で拒否/無視される。ページ自体（`/live-schedule`・ライブ結果ページ）は未ログインでも閲覧・シェアできる（Xでの拡散を想定した意図的な設計）。

---

## 9. 管理者機能（`/admin`配下）

`profiles.role='admin'`のユーザーのみアクセス可能。

| 画面 | 内容 |
|---|---|
| `/admin/schedule` | ライブ予定の作成・編集・「前回/今回/次回」の表示先割当 |
| `/admin/topics` | お題バンクの管理（追加・編集・使用停止） |
| `/admin/posts` | 寄合帳の投稿モデレーション（非表示・削除） |
| `/admin/reports` | 通報のレビューキュー |
| `/admin/users`・`/admin/users/[id]` | ユーザー管理（警告・期限付き/永久停止・解除・アカウント完全削除） |
| `/admin/notifications` | お知らせの一斉配信、各種通知の非表示操作 |
| `/admin/live-results`・`/admin/live-results/[liveId]` | 寄合帳へのライブ結果掲載内容のキュレーション |
| `/admin/logs` | 運営操作の履歴（誰が・いつ・何をしたか） |

アカウントの完全削除は、専用のAPIルート（`/api/admin/users/[id]/delete`）経由でSupabaseのユーザー管理権限を使って実行され、`auth.users`から実際に削除される（取り消せない操作）。

---

## 10. デザインについて（現状は3系統が併存）

1. **Stadium系**（地下ライブハウス風、紙もの・活版インクの質感）：ホーム・マイページ・寄合帳・ライブ予定・遊び方
2. **ネオ寄席**（暗色＋暖色スポットライト、緞帳赤・幕金）：ライブ本体（`/live`、`/live/host`）
3. **和風畳色**：現在「近日公開」にしている旧機能（ガチャ・楽屋・番付表）

この3系統の統一は今のところ意図的に見送っています（デザイン部レビューでは「ホームは日常、ライブは非日常」という住み分けとして成立し得るという評価。詳細は別途のレビュー記録を参照）。

---

## 11. データモデル概要（実際に存在する主なテーブル）

```
lives ── groups ── turns ── answers ── scores
  └ participants（live_idごとの参加者、role/preferred_role/group_id）
  └ topics（そのライブ用に払い出されたお題本体）

topic_bank（お題の元ネタ、複数ライブで使い回す）
live_schedule_entries（ホーム/ライブ予定ページの表示専用データ）

profiles（ユーザー本体：段位/ポイント/寄合券/停止状態/権限など）
point_history（ポイント加算履歴）

sns_topics / sns_answers / sns_comments（寄合帳の投稿本体）
sns_answer_likes / sns_follows（いいね・フォロー）
sns_live_results / sns_live_result_answers / sns_live_result_likes / sns_live_result_comments（ライブ結果掲載）

notifications（ベル通知）
user_sanctions（警告・停止の履歴）
reports（通報）
admin_action_logs（運営操作履歴）

live_tsukkomi_events（ライブ中のツッコミ/拍手、演出専用）
share_click_events（Xシェアのクリック計測）
```

各テーブルの列レベルの詳細は`supabase/migrations/`(0001〜0046)を参照してください。本書はあくまで機能仕様としての概要です。

---

## 12. 今後の展開（未実装・保留中のもの）

以下は企画部との壁打ちで出た方向性で、**まだ実装されていません**。優先順位は別途相談のうえ決めてください。

- 経済の一本化：ガチャ/ショップ/楽屋を`profiles.points_balance`ベースに作り直して再解禁
- ライブ報酬アイテム配布（表彰・参加でアイテムが増える仕組み）
- ライブ開始のベル通知配信（開催通知トグルは現在ダミーのため撤去済み）
- 寄合帳のお題をお題バンクへ運営が昇格できる導線
- OGP（Xでシェアした時の画像付きリンクカード）対応
- 月次番付、複数ライブの同時開催、進行の完全自動化 等（いずれも要検討・未着手）

---

## 13. 本書の位置づけ

本書は2026-09-02時点のコードを基準にしています。今後の変更で古くなった場合は、その都度この文書自体を更新するか、新しいバージョンに差し替えてください（旧`仕様書.md`をそのまま放置して食い違いが広がった反省を踏まえ、更新を怠らないようにしてください）。
